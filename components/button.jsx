import React from 'react'
import styled from 'styled-components'

const styledButton = styled.button`
  background-color: 
`

export default function Button ({ symbol }) {
  return (
    <button> {symbol} </button>
  )
}
